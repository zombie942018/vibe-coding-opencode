# vibe-coding-opencode

訓練 vibe coding / 專案用

一個**小型網頁工具集合**的 repo。每個工具都是「單一 HTML 檔」，HTML / CSS / JavaScript 全部包在同一個檔案裡，直接用瀏覽器開啟即可使用，不需安裝任何套件。

---

## 📦 工具列表

| 工具 | 檔案 | 目前版本 | 核心功能 |
|---|---|---|---|
| 📚 隨機單字卡 | `flashcard.html` | v1.2.0 | 隨機抽英文單字、點擊翻面看中文翻譯、可上/下一張切換 |

> 新增工具時，記得更新上表與下方的詳細說明。

---

## 🔧 工具詳細說明

### 📚 隨機單字卡（`flashcard.html`）

**版本：** v1.2.0

**核心功能：**

- 內建 10 個常用英文單字，每個單字包含：英文、詞性、中文翻譯、英/中應用情境例句
- 卡片預設顯示英文，點擊卡片本體或「翻面」按鈕可切換中/英文（3D 翻轉動畫）
- 「下一張」按鈕隨機抽卡，採「洗牌 + 依序發牌」機制，確保一輪 10 張內不重複、也不會連續抽到同一張
- 「上一張」按鈕可返回先前看過的卡片，與下一張相同設計
- 進度顯示（第 X / 10 張），抽完一輪自動重新洗牌
- 鍵盤支援：空白鍵翻面、Enter / → 下一張、← 上一張
- 響應式設計，手機與電腦皆可使用

**操作方式：**

| 操作 | 方式 |
|---|---|
| 翻面 | 點擊卡片 或「翻面」按鈕（或按空白鍵） |
| 上一張 | 「上一張」按鈕（或按 ←） |
| 下一張 | 「下一張」按鈕（或按 Enter / →） |

**擴充單字：** 編輯檔案中 `const WORDS = [...]` 區塊，照格式新增物件即可（`en` / `pos` / `zh` / `exEn` / `exZh` 五個欄位）。

---

## 📝 更新日誌（Changelog）

> 依「最新在上」的順序記錄，每筆包含版本號、日期與變更內容。

### v1.2.0 — 2026-08-02
- **新增**：「上一張」按鈕，可返回先前看過的卡片（與下一張相同設計）
- **調整**：「下一題」更名為「下一張」，並移除骰子圖示
- **調整**：鍵盤新增 ← 返回上一張；進度顯示由「第 X / 10 題」改為「第 X / 10 張」
- **調整**：快捷鍵提示改為按鍵方框（kbd）樣式，獨立顯示於畫面右下角，並補上 → 下一張、↑/↓ 翻面
- **重構**：抽牌邏輯改為「順序陣列 + 位置指標」，支援在洗牌順序中來回走訪

### v1.1.0 — 2026-08-02
- **修正**：題數顯示原本是倒數（第一題顯示 9/10），改為正向計數（第一題顯示 1/10），並新增「剩餘單字數」欄位

### v1.0.0 — 2026-07-31
- 初始版本：隨機單字卡工具正式發布
- 內建 10 個單字、卡片翻面、隨機抽題（不重複）、鍵盤操作、響應式版面

---

## 🗂 維護備忘（給未來的自己）

- **新增工具**：把單一 HTML 檔放進 repo → 更新「工具列表」表格 → 新增「工具詳細說明」區塊 → 在更新日誌加一筆 `v1.0.0`（工具級版本從 1.0.0 開始）。
- **修改既有工具**：工具內的變更直接寫進更新日誌，版本號按語意化版本規則遞增：
  - `vX.Y.Z`：Z = bug 修正；Y = 新增功能；X = 重大改版（介面或行為不兼容）。
- **推上 GitHub**：只要 commit 並 push，`README.md` 就會同步覆蓋遠端的舊版，不需要額外動作。
